[project]
name = "xplane-pilot"
version = "0.1.0"
description = "Deterministic milestone 1 X-Plane pilot core"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
sim-pilot = "sim_pilot.__main__:main"
